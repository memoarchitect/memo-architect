const fs = require('fs'); const content = fs.readFileSync('../memo/examples/gpca-pump/.memo/cache/compiler-output.json', 'utf8'); const data = JSON.parse(content); const kinds = data.registries.kinds; const sysFunc = kinds.find(k => k.name === 'SystemFunction'); console.log('SystemFunction namespace:', sysFunc?.namespace, 'layer:', sysFunc?.layer); const acq = kinds.find(k => k.name === 'AcquireSensorData'); console.log('AcquireSensorData namespace:', acq?.namespace, 'layer:', acq?.layer);
