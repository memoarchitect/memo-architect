# Phase 1 — Ontology Cleanup (P1, demoted from P0)

Simplify the monolithic ontology. Extract technology-specific defs, collapse over-split types, move niche concepts to extensions.
Target: ~140 core + ~150 medical = ~290 total (down from 375).

## Open Issues

