# Infusion Pump Project — example device model
projectName: infusion-pump
projectType: device

extends: "@memo/medical-modeling-profile"

# Default: only core + medical are selected.
# Enable specialized ontologies as your project needs them.
ontologies:
  - name: "@memo/ontology-core"
    version: "^0.1.0"
  - name: "@memo/ontology-medical"
    version: "^0.1.0"
