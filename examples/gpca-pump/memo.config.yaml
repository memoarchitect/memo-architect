# GPCA Infusion Pump — submission-grade reference model
# Based on University of Minnesota CriSys GPCA project
projectName: gpca-pump
projectType: device

# Phase C: project pins one methodology. The methodology package
# (@memo/methodology-default) extends @memo/ontology-arch, which
# pulls in architecture kinds and relationships from the top-level ontology/.
methodology: "@memo/methodology-default@^1.0"
