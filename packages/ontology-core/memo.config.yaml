projectName: "@memo/ontology-core"
projectType: ontology

ontologyMetadata:
  id: "@memo/ontology-core"
  version: "0.1.0"
  description: "Domain-agnostic MBSE backbone ontology aligned to ISO/IEC/IEEE 42010"
  author: "SysAnd"
  license: "Apache-2.0"
  tags:
    - mbse
    - sysml-v2
    - architecture
    - 42010

cosmaLayers:
  - id: purpose
    label: Purpose & Stakeholders
    color: "#8E44AD"

  - id: operational
    label: Operational Analysis
    color: "#C0392B"

  - id: requirements
    label: Requirements
    color: "#4A90D9"

  - id: functional
    label: Functional Analysis
    color: "#E67E22"

  - id: logical
    label: Logical Architecture
    color: "#7B68EE"

  - id: physical
    label: Physical Architecture
    color: "#95A5A6"

  - id: software
    label: Software Architecture
    color: "#F39C12"

  - id: interfaces
    label: Interfaces & Contracts
    color: "#1ABC9C"

  - id: analysis
    label: Analysis & Constraints
    color: "#16A085"

  - id: verification
    label: Verification
    color: "#2ECC71"

kinds:
  Program:
    label: Program
    layer: purpose
    sysmlConstruct: part def

  Actor:
    label: Actor
    layer: purpose
    sysmlConstruct: part def

  Stakeholder:
    label: Stakeholder
    layer: purpose
    sysmlConstruct: part def

  Goal:
    label: Goal
    layer: purpose
    sysmlConstruct: requirement def

  Concern:
    label: Concern
    layer: purpose
    sysmlConstruct: requirement def

  Capability:
    label: Capability
    layer: purpose
    sysmlConstruct: part def

  Mission:
    label: Mission
    layer: operational
    sysmlConstruct: part def

  Context:
    label: Context
    layer: operational
    sysmlConstruct: part def

  OperationalActor:
    label: Operational Actor
    layer: operational
    sysmlConstruct: part def

  OperationalEntity:
    label: Operational Entity
    layer: operational
    sysmlConstruct: part def

  OperationalEnvironment:
    label: Operational Environment
    layer: operational
    sysmlConstruct: part def

  Resource:
    label: Resource
    layer: operational
    sysmlConstruct: part def

  Substance:
    label: Substance
    layer: operational
    sysmlConstruct: part def

  Observable:
    label: Observable
    layer: operational
    sysmlConstruct: part def

  OperationalActivity:
    label: Operational Activity
    layer: operational
    sysmlConstruct: action def

  Operation:
    label: Operation
    layer: operational
    sysmlConstruct: action def

  Procedure:
    label: Procedure
    layer: operational
    sysmlConstruct: action def

  OperationalScenario:
    label: Operational Scenario
    layer: operational
    sysmlConstruct: action def

  MissionPhase:
    label: Mission Phase
    layer: operational
    sysmlConstruct: part def

  Requirement:
    label: Requirement
    layer: requirements
    sysmlConstruct: requirement def

  StakeholderNeed:
    label: Stakeholder Need
    layer: requirements
    sysmlConstruct: requirement def

  MissionRequirement:
    label: Mission Requirement
    layer: requirements
    sysmlConstruct: requirement def

  SystemRequirement:
    label: System Requirement
    layer: requirements
    sysmlConstruct: requirement def

  FunctionalRequirement:
    label: Functional Requirement
    layer: requirements
    sysmlConstruct: requirement def

  TechnicalRequirement:
    label: Technical Requirement
    layer: requirements
    sysmlConstruct: requirement def

  InterfaceRequirement:
    label: Interface Requirement
    layer: requirements
    sysmlConstruct: requirement def

  DesignSpecification:
    label: Design Specification
    layer: requirements
    sysmlConstruct: requirement def

  Specification:
    label: Specification
    layer: requirements
    sysmlConstruct: requirement def

  MissionSpecification:
    label: Mission Specification
    layer: requirements
    sysmlConstruct: requirement def

  FunctionSpecification:
    label: Function Specification
    layer: requirements
    sysmlConstruct: requirement def

  SystemSpecification:
    label: System Specification
    layer: requirements
    sysmlConstruct: requirement def

  RequirementSet:
    label: Requirement Set
    layer: requirements
    sysmlConstruct: part def

  Function:
    label: Function
    layer: functional
    sysmlConstruct: action def

  MissionFunction:
    label: Mission Function
    layer: functional
    sysmlConstruct: action def

  SystemFunction:
    label: System Function
    layer: functional
    sysmlConstruct: action def

  ComponentFunction:
    label: Component Function
    layer: functional
    sysmlConstruct: action def

  UseCase:
    label: Use Case
    layer: functional
    sysmlConstruct: part def

  Scenario:
    label: Scenario
    layer: functional
    sysmlConstruct: action def

  System:
    label: System
    layer: logical
    sysmlConstruct: part def

  SystemOfSystems:
    label: System Of Systems
    layer: logical
    sysmlConstruct: part def

  SystemExternal:
    label: External System
    layer: logical
    sysmlConstruct: part def

  Subsystem:
    label: Subsystem
    layer: logical
    sysmlConstruct: part def

  LogicalComponent:
    label: Logical Component
    layer: logical
    sysmlConstruct: part def

  ArchitectureDecision:
    label: Architecture Decision
    layer: logical
    sysmlConstruct: part def

  ArchitectureRationale:
    label: Architecture Rationale
    layer: logical
    sysmlConstruct: part def

  QualityAttribute:
    label: Quality Attribute
    layer: logical
    sysmlConstruct: part def

  Question:
    label: Question
    layer: logical
    sysmlConstruct: part def

  PhysicalComponent:
    label: Physical Component
    layer: physical
    sysmlConstruct: part def

  ElectricalComponent:
    label: Electrical Component
    layer: physical
    sysmlConstruct: part def

  MechanicalComponent:
    label: Mechanical Component
    layer: physical
    sysmlConstruct: part def

  PhysicalModule:
    label: Physical Module
    layer: physical
    sysmlConstruct: part def

  HardwareNode:
    label: Hardware Node
    layer: physical
    sysmlConstruct: part def

  ComputingDevice:
    label: Computing Device
    layer: physical
    sysmlConstruct: part def

  FPGA:
    label: FPGA
    layer: physical
    sysmlConstruct: part def

  Microcontroller:
    label: Microcontroller
    layer: physical
    sysmlConstruct: part def

  SingleBoardComputer:
    label: Single Board Computer
    layer: physical
    sysmlConstruct: part def

  Software:
    label: Software
    layer: software
    sysmlConstruct: part def

  SoftwareComponent:
    label: Software Component
    layer: software
    sysmlConstruct: part def

  SoftwareModule:
    label: Software Module
    layer: software
    sysmlConstruct: part def

  Firmware:
    label: Firmware
    layer: software
    sysmlConstruct: part def

  OperatingSystem:
    label: Operating System
    layer: software
    sysmlConstruct: part def

  Docker:
    label: Docker
    layer: software
    sysmlConstruct: part def

  EventDrivenService:
    label: Event Driven Service
    layer: software
    sysmlConstruct: part def

  MessageBroker:
    label: Message Broker
    layer: software
    sysmlConstruct: part def

  Port:
    label: Port
    layer: interfaces
    sysmlConstruct: port def

  PortEthernet:
    label: Ethernet Port
    layer: interfaces
    sysmlConstruct: port def

  PortUSB:
    label: USB Port
    layer: interfaces
    sysmlConstruct: port def

  PortSerial:
    label: Serial Port
    layer: interfaces
    sysmlConstruct: port def

  PortPower:
    label: Power Port
    layer: interfaces
    sysmlConstruct: port def

  DataEndpoint:
    label: Data Endpoint
    layer: interfaces
    sysmlConstruct: port def

  Interface:
    label: Interface
    layer: interfaces
    sysmlConstruct: interface def

  DataInterface:
    label: Data Interface
    layer: interfaces
    sysmlConstruct: interface def

  PublishSubscribeChannel:
    label: Publish Subscribe Channel
    layer: interfaces
    sysmlConstruct: interface def

  EventBus:
    label: Event Bus
    layer: interfaces
    sysmlConstruct: interface def

  TopicChannel:
    label: Topic Channel
    layer: interfaces
    sysmlConstruct: interface def

  QueueChannel:
    label: Queue Channel
    layer: interfaces
    sysmlConstruct: interface def

  RequestResponseInterface:
    label: Request Response Interface
    layer: interfaces
    sysmlConstruct: interface def

  SoftwareInterface:
    label: Software Interface
    layer: interfaces
    sysmlConstruct: interface def

  SoftwareProvidedInterface:
    label: Software Provided Interface
    layer: interfaces
    sysmlConstruct: interface def

  SoftwareRequiredInterface:
    label: Software Required Interface
    layer: interfaces
    sysmlConstruct: interface def

  PublisherInterface:
    label: Publisher Interface
    layer: interfaces
    sysmlConstruct: interface def

  SubscriberInterface:
    label: Subscriber Interface
    layer: interfaces
    sysmlConstruct: interface def

  ServiceProviderInterface:
    label: Service Provider Interface
    layer: interfaces
    sysmlConstruct: interface def

  ServiceConsumerInterface:
    label: Service Consumer Interface
    layer: interfaces
    sysmlConstruct: interface def

  MessageProducer:
    label: Message Producer
    layer: interfaces
    sysmlConstruct: interface def

  MessageConsumer:
    label: Message Consumer
    layer: interfaces
    sysmlConstruct: interface def

  BrokerExchange:
    label: Broker Exchange
    layer: interfaces
    sysmlConstruct: interface def

  BrokerQueue:
    label: Broker Queue
    layer: interfaces
    sysmlConstruct: interface def

  InterfaceContract:
    label: Interface Contract
    layer: interfaces
    sysmlConstruct: interface def

  CommunicationProtocol:
    label: Communication Protocol
    layer: interfaces
    sysmlConstruct: interface def

  InteroperabilityProfile:
    label: Interoperability Profile
    layer: interfaces
    sysmlConstruct: interface def

  ExchangeItem:
    label: Exchange Item
    layer: interfaces
    sysmlConstruct: item def

  Message:
    label: Message
    layer: interfaces
    sysmlConstruct: item def

  EventMessage:
    label: Event Message
    layer: interfaces
    sysmlConstruct: item def

  StateMessage:
    label: State Message
    layer: interfaces
    sysmlConstruct: item def

  CommandMessage:
    label: Command Message
    layer: interfaces
    sysmlConstruct: item def

  RequestMessage:
    label: Request Message
    layer: interfaces
    sysmlConstruct: item def

  ResponseMessage:
    label: Response Message
    layer: interfaces
    sysmlConstruct: item def

  MessageSchema:
    label: Message Schema
    layer: interfaces
    sysmlConstruct: part def

  MessageField:
    label: Message Field
    layer: interfaces
    sysmlConstruct: part def

  RosNode:
    label: ROS Node
    layer: software
    sysmlConstruct: part def

  RosTopic:
    label: ROS Topic
    layer: interfaces
    sysmlConstruct: interface def

  RosService:
    label: ROS Service
    layer: interfaces
    sysmlConstruct: interface def

  RosPublication:
    label: ROS Publication
    layer: interfaces
    sysmlConstruct: interface def

  RosSubscription:
    label: ROS Subscription
    layer: interfaces
    sysmlConstruct: interface def

  RosServiceCall:
    label: ROS Service Call
    layer: interfaces
    sysmlConstruct: interface def

  RosServiceServer:
    label: ROS Service Server
    layer: interfaces
    sysmlConstruct: interface def

  RosMessage:
    label: ROS Message
    layer: interfaces
    sysmlConstruct: item def

  RosEventMessage:
    label: ROS Event Message
    layer: interfaces
    sysmlConstruct: item def

  RosStateMessage:
    label: ROS State Message
    layer: interfaces
    sysmlConstruct: item def

  RosCommandMessage:
    label: ROS Command Message
    layer: interfaces
    sysmlConstruct: item def

  RosRequest:
    label: ROS Request
    layer: interfaces
    sysmlConstruct: item def

  RosResponse:
    label: ROS Response
    layer: interfaces
    sysmlConstruct: item def

  RosMessageSchema:
    label: ROS Message Schema
    layer: interfaces
    sysmlConstruct: part def

  RosMessageField:
    label: ROS Message Field
    layer: interfaces
    sysmlConstruct: part def

  RabbitMqBroker:
    label: RabbitMQ Broker
    layer: software
    sysmlConstruct: part def

  RabbitMqExchange:
    label: RabbitMQ Exchange
    layer: interfaces
    sysmlConstruct: interface def

  RabbitMqQueue:
    label: RabbitMQ Queue
    layer: interfaces
    sysmlConstruct: interface def

  RabbitMqPublisher:
    label: RabbitMQ Publisher
    layer: interfaces
    sysmlConstruct: interface def

  RabbitMqConsumer:
    label: RabbitMQ Consumer
    layer: interfaces
    sysmlConstruct: interface def

  DataType:
    label: Data Type
    layer: interfaces
    sysmlConstruct: attribute def

  AnalysisCase:
    label: Analysis Case
    layer: analysis
    sysmlConstruct: part def

  Constraint:
    label: Constraint
    layer: analysis
    sysmlConstruct: part def

  Calculation:
    label: Calculation
    layer: analysis
    sysmlConstruct: part def

  TradeStudy:
    label: Trade Study
    layer: analysis
    sysmlConstruct: part def

  AnalysisResult:
    label: Analysis Result
    layer: analysis
    sysmlConstruct: part def

  Assumption:
    label: Assumption
    layer: analysis
    sysmlConstruct: part def

  Measure:
    label: Measure
    layer: analysis
    sysmlConstruct: part def

  Parameter:
    label: Parameter
    layer: analysis
    sysmlConstruct: attribute def

  Test:
    label: Test
    layer: verification
    sysmlConstruct: part def

  VerificationCase:
    label: Verification Case
    layer: verification
    sysmlConstruct: part def

  ValidationCase:
    label: Validation Case
    layer: verification
    sysmlConstruct: part def

  Evidence:
    label: Evidence
    layer: verification
    sysmlConstruct: part def

relationshipTypes:
  - name: aggregation
    label: Aggregation
    layer: logical
    color: "#7B68EE"

  - name: association
    label: Association
    layer: logical
    color: "#9B59B6"

  - name: traceTo
    label: Trace To
    layer: requirements
    color: "#4A90D9"

  - name: trace
    label: Trace
    layer: requirements
    color: "#3498DB"

  - name: refines
    label: Refines
    layer: requirements
    color: "#2E86C1"

  - name: derives
    label: Derives
    layer: requirements
    color: "#5DADE2"

  - name: constrains
    label: Constrains
    layer: analysis
    color: "#16A085"

  - name: allocateTo
    label: Allocate To
    layer: functional
    color: "#E67E22"

  - name: composedOf
    label: Composed Of
    layer: logical
    color: "#8E44AD"

  - name: decomposedBy
    label: Decomposed By
    layer: functional
    color: "#D35400"

  - name: dependency
    label: Dependency
    layer: logical
    color: "#95A5A6"

  - name: participatesInSystemOfSystems
    label: Participates In System Of Systems
    layer: logical
    color: "#7D3C98"

  - name: exposesInterface
    label: Exposes Interface
    layer: interfaces
    color: "#17A589"

  - name: hasEndpoint
    label: Has Endpoint
    layer: interfaces
    color: "#148F77"

  - name: implementsProtocol
    label: Implements Protocol
    layer: interfaces
    color: "#117864"

  - name: conformsToProfile
    label: Conforms To Profile
    layer: interfaces
    color: "#0E6251"

  - name: carriesExchangeItem
    label: Carries Exchange Item
    layer: interfaces
    color: "#1ABC9C"

  - name: publishesTo
    label: Publishes To
    layer: interfaces
    color: "#16A085"

  - name: subscribesTo
    label: Subscribes To
    layer: interfaces
    color: "#148F77"

  - name: publishesMessage
    label: Publishes Message
    layer: interfaces
    color: "#117864"

  - name: consumesMessage
    label: Consumes Message
    layer: interfaces
    color: "#0E6251"

  - name: servesInterface
    label: Serves Interface
    layer: interfaces
    color: "#1F618D"

  - name: invokesInterface
    label: Invokes Interface
    layer: interfaces
    color: "#2874A6"

  - name: carriesRequestMessage
    label: Carries Request Message
    layer: interfaces
    color: "#2E86C1"

  - name: carriesResponseMessage
    label: Carries Response Message
    layer: interfaces
    color: "#3498DB"

  - name: brokersChannel
    label: Brokers Channel
    layer: interfaces
    color: "#5DADE2"

  - name: routesToChannel
    label: Routes To Channel
    layer: interfaces
    color: "#85C1E9"

  - name: definesMessageSchema
    label: Defines Message Schema
    layer: interfaces
    color: "#48C9B0"

  - name: hasMessageField
    label: Has Message Field
    layer: interfaces
    color: "#45B39D"

  - name: realization
    label: Realization
    layer: logical
    color: "#2C3E50"

  - name: satisfy
    label: Satisfy
    layer: requirements
    color: "#2ECC71"

  - name: verify
    label: Verify
    layer: verification
    color: "#27AE60"

  - name: flow
    label: Flow
    layer: interfaces
    color: "#1ABC9C"

  - name: hasSubProcedure
    label: Has Subprocedure
    layer: operational
    color: "#CD6155"

  - name: performedBy
    label: Performed By
    layer: operational
    color: "#C0392B"

  - name: performedOn
    label: Performed On
    layer: operational
    color: "#A93226"

  - name: occursIn
    label: Occurs In
    layer: operational
    color: "#922B21"

  - name: usesResource
    label: Uses Resource
    layer: operational
    color: "#E74C3C"

  - name: hasEquipment
    label: Has Equipment
    layer: operational
    color: "#CB4335"

  - name: hasPersonnel
    label: Has Personnel
    layer: operational
    color: "#B03A2E"

  - name: containsSubstance
    label: Contains Substance
    layer: operational
    color: "#884EA0"

  - name: succession
    label: Succession
    layer: operational
    color: "#C0392B"

relationships:
  - aggregation
  - association
  - traceTo
  - trace
  - refines
  - derives
  - constrains
  - allocateTo
  - composedOf
  - decomposedBy
  - dependency
  - participatesInSystemOfSystems
  - exposesInterface
  - hasEndpoint
  - implementsProtocol
  - conformsToProfile
  - carriesExchangeItem
  - publishesTo
  - subscribesTo
  - publishesMessage
  - consumesMessage
  - servesInterface
  - invokesInterface
  - carriesRequestMessage
  - carriesResponseMessage
  - brokersChannel
  - routesToChannel
  - definesMessageSchema
  - hasMessageField
  - realization
  - satisfy
  - verify
  - flow
  - hasSubProcedure
  - performedBy
  - performedOn
  - occursIn
  - usesResource
  - hasEquipment
  - hasPersonnel
  - containsSubstance
  - succession

closureRules: []
