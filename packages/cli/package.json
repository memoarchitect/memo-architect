{
  "name": "@memo/cli",
  "version": "0.1.0",
  "description": "MEMO CLI — init, dev, validate, build, pack commands for medical device MBSE",
  "type": "module",
  "bin": {
    "memo": "./lib/bin/memo.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf lib/"
  },
  "dependencies": {
    "@memo/core": "workspace:*",
    "commander": "^12.1.0",
    "chokidar": "^4.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.0",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "typescript": "~5.5.0",
    "vitest": "^2.1.0"
  }
}
